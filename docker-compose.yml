version: "3.8"

services:
  ai-captioner:
    build: .
    container_name: ai-captioner
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
